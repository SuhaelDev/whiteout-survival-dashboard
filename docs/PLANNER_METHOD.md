# Planner Method (Deterministic v1) - 2026-07-26T18:56:09Z
Pipeline: atomic next-step candidates per module (buildings, chief gear, charms, pets, experts incl. Gareth, hero gear +10 blocks) -> exact cost vectors from verified tables -> exchange-aware feasibility (choice conversions allowed, random containers excluded) -> reservations subtracted from availability -> transparent scoring.
score = utility / (scarcityCost x shortfallPenalty); utility = module benefitValue (stat-derived); scarcityCost = per-resource weights (editable via state.planner_weights); shortfallPenalty = 1 + 0.35 x missing-material-kinds. Components displayed on every row.
Objectives shipped: Best-available-now (strict inventory), Highest value per scarcity cost, Best SvS value (verified svs points / scarcity cost). Steps with non-game-verified data are quarantined in an "Insufficient verified data" section and never ranked. Determinism: pure functions of state + static data (verified two-run identity test). LLM advisor is explanation-only, cannot alter numbers. Roadmap v2: prerequisite closure graph, multi-step horizon plans (7/30-day), acquisition-rate procurement times, role presets.
